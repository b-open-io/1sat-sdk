# Changelog

## 0.0.10

### Added
- Re-export all templates from `@1sat/templates` (OrdLockTemplate, InscriptionTemplate, Lock, BSV20, BSV21, AIP, BAP, MAPTemplate, SigmaTemplate, B, BitCom, BSocial)
- Re-export Signer abstraction (PrivateKeySigner, WalletSigner)
- Re-export all template types, constants, and enums

### Changed
- Internal deps now use workspace:* for consistent types across monorepo
